{
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "dev-w": "bun run --watch src/server.ts"
  },
  "dependencies": {
    "hono": "^4.0.2",
    "mongoose": "^8.1.2",
    "short-unique-id": "^5.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
